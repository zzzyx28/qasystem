# =============================================================================
# 前端（Vite）本地开发 / 构建
#
# 当前项目 API 使用相对路径 /api，开发环境由 vite.config.js 代理到后端。
# 默认后端地址为 http://localhost:8000（与项目 run.sh 保持一致）。
# 如需修改后端地址/端口，请配置：
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
#
# 其他 Vite 环境变量请以 VITE_ 前缀声明，例如：
# VITE_APP_TITLE=轨道交通知识服务
# =============================================================================
